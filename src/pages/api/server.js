// configuração de conexão api
const porta = 2112;
const endereco = "http://10.20.46.32"; // Em casa mudar para o ip da maquina
const serverApi = `http://${endereco}:${porta}`;

export default serverApi;
